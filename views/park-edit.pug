extends layout.pug

include utils.pug

block content
  +validationErrorSummary(errors)
  form(action=`/park/edit/${park.id}` method='post')
    include park-form-fields.pug
    div(class='py-4')
      button(class='btn btn-primary' type='submit') Update Park
      a(class='btn btn-warning ml-2' href=`/park/${park.id}` role='button') Cancel